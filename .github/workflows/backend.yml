name: backend

on:
  push:
    paths:
    - ".github/workflows/backend.yml"
    - "CMakeLists.txt"
    - "src/*"
    - "scripts/*"
  pull_request:
    paths:
    - ".github/workflows/backend.yml"
    - "CMakeLists.txt"
    - "src/*"
    - "scripts/*"
  workflow_call:

jobs:
  cross:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64]
    steps:
      - uses: actions/checkout@v4
      - name: use freebsd-vm action
        uses: vmactions/freebsd-vm@v1
        env:
          BUILD_TARGET: ${{ matrix.target }}
        with:
          usesh: true
          envs: 'BUILD_TARGET'
          prepare: |
            pkg install -y autoconf automake cmake curl file libtool

          run: |
            pwd
            ls -la
            ./scripts/cross-build.sh
      - uses: actions/upload-artifact@v4
        with:
          name: ttyd.${{ matrix.target }}
          path: build/ttyd*

